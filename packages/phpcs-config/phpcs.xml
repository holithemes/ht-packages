<?xml version="1.0"?>
<ruleset name="HoliThemes - WordPress Coding Standards">
  <description>WordPress PHP code style for HoliThemes projects.</description>

  <!-- Include WordPress core rules -->
  <rule ref="WordPress">
    <!-- Load only the Security category -->
    <rule ref="WordPress.Security"/>
    <!-- Be extra strict on prepared SQL -->
    <rule ref="WordPress.DB.PreparedSQL"/>
    <rule ref="WordPress.DB.PreparedSQLPlaceholders"/>

    <!-- BYPASS: commented-out code detector -->
    <exclude name="Squiz.PHP.CommentedOutCode"/>
    <exclude name="Generic.CodeAnalysis.EmptyStatement.DetectedElse"/>
  </rule>

  <!-- Files/folders to scan -->
  <file>./</file>

  <!-- Exclude paths -->
  <exclude-pattern>vendor/*</exclude-pattern>
  <exclude-pattern>node_modules/*</exclude-pattern>

  <!-- Disable specific rules -->
  <rule ref="">
    <severity>0</severity>
  </rule>
  
  <!-- Ignore non-PHP assets -->
  <exclude-pattern>*.min.js</exclude-pattern>
  <exclude-pattern>*.js</exclude-pattern>
  <exclude-pattern>*.css</exclude-pattern>
  <exclude-pattern>*.html</exclude-pattern>
  <exclude-pattern>*.xml</exclude-pattern>

  <!-- Custom settings -->
  <config name="tab-width" value="4"/>
</ruleset>